{
  "name": "peopleconnecthr-ats",
  "private": true,
  "scripts": {
    "postinstall": "npm install --omit=dev --prefix backend",
    "build": "npm install --omit=dev --prefix backend",
    "start": "cd backend && node --max-old-space-size=4096 server.js"
  },
  "dependencies": {
    "@railway/cli": "^5.30.1"
  }
}
