import React from 'react';
import { StatCard } from '../dashboard/DashboardWidgets';

/** Analytics KPI cards — same component styling as the main Dashboard. */
export default function KpiCard(props) {
  return <StatCard {...props} />;
}
